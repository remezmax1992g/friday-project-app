import React from 'react';
import {NavLink} from "react-router-dom";

const Profile = () => {
    return (
        <NavLink to="profile">
            <div>
                Profile
            </div>
        </NavLink>
    );
};

export default Profile;